# TODO

## Deferred Governance Follow-Ups

- Rewrite `AGENTS.md` so binding instructions are not stored under `# Considerations`.
- Remove redundant `Violation:` glosses from `CLAUDE.md` and normalize its formatting to the governance-schema style.
- Normalize remaining governed Markdown files that still use duplicated placeholder subsection headings such as `## Invariants` or `## Considerations`.
- Revisit `.agent/schemas/handoff-protocol.md` recovery handling for governance-file changes, including whether `stash` should replace partial discard semantics.
- Revisit the ambiguity around how to judge related worktrees during bootstrap.
- Revisit whether `.agent/schemas/engineering-protocol.md` beliefs need tighter scope and override language.
