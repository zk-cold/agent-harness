# Invariants

## Fast-Path Eligibility Verification
Fast-path eligibility requires either of the following:
- The mission delivery mode is `Test-Only`.
- No more than three governance artifacts are modified or removed, no considerations are overridden, and no more than 7 git-tracked files have uncommitted changes.
