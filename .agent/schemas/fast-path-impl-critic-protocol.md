# Invariants

## Fast-Path Eligibility Verification
Reject if
- More than one governance artifact is created/modified/removed, OR
- There are overrides to considerations or bliefs, OR
- At least 5 git-tracked files have uncommitted changes
