# Mission: <title>
## Delivery Mode
<!-- Exactly one of: TDD, TDD-exempt, Test-Only -->
<!-- For Test-Only, each proposed invariant or external constraint must state [Already-Satisfied Behavior]. -->
<!-- All sections below are optional; include only when applicable. Omit entirely otherwise. -->
<!--
Each proposed governance artifact (Invariant, External Constraint, Consideration) may end with a trailing italics line declaring its persistence:
  _transient_                          — applies during the mission only.
  _persist: <governed-document>_       — persisted as a prose rule in the named governed document.
Invariants and External Constraints without such a line default to test persistence. A Consideration without such a line is _transient_ by default; declare _persist:_ only when its lasting value is self-evident from the rule.
-->
## Invariants 
## External Constraints
## Considerations
## Assumptions
## Out of scope
<!-- Bullets of related work explicitly excluded. -->
